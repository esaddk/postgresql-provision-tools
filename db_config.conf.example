#!/bin/bash

# PostgreSQL Database Configuration Template
# Copy this file to db_config.conf and modify the values as needed

# Database Configuration
DBNAME="myapp_db"

# Usernames
USER_APP="myapp_app"
USER_OWNER="myapp_owner"

# Role Names
ROLE_RW="r_myapp_readwrite"
ROLE_RC="r_myapp_creator"

# PostgreSQL Connection Settings
PG_USER="postgres"
PG_HOST="localhost"
PG_PORT="5432"

# PostgreSQL Password (optional, will be prompted if not provided)
# PG_PASSWORD="your_postgres_password"

# User Passwords (optional, will be auto-generated if not provided)
# USER_APP_PASSWORD="custom_app_password"
# USER_OWNER_PASSWORD="custom_owner_password"

# Connection Testing (Mandatory)
# Test host can be either load balancer IP or direct instance IP
TEST_HOST="localhost"
TEST_PORT="5432"

# Optional: Custom credential file name (overrides auto-generated filename)
# SAVE_CREDENTIALS_FILE="my_custom_credentials.txt"

# Note: Credentials are automatically saved to cred_{DBNAME}_{DDMMYYYY}.txt
# when passwords are auto-generated, unless SAVE_CREDENTIALS_FILE is specified